name: MySQL Migrations

on: [push]

jobs:
  migration:
    runs-on: ubuntu-latest
    steps:
      - name: Checking out code from the repository
        uses: actions/checkout@v2
      - name: Compose
        uses: isbang/compose-action@v1.3.2
        with:
          compose-file: "./migrate/docker-compose.yml"




